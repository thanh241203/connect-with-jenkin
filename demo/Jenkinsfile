pipeline {
    agent any

    environment {
        DOCKERHUB_USER = 'thanhkon241203265'
        DOCKERHUB_PASS = credentials('dockerhub-credentials')  // lưu trên Jenkins
        IMAGE_NAME = 'thanhkon241203265/ks-app'
    }
    tools {
        maven 'MAVEN_HOME'
        jdk 'JDK17'
    }

    stages {
        stage('Checkout') {
            steps {
                echo '📦 Cloning GitHub repository...'
                git branch: 'master', url: 'https://github.com/thanh241203/connect-with-jenkin.git'
            }
        }

        stage('Build') {
            steps {
                echo '🔨 Building the project...'
                dir('demo'){
                    bat 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                dir('demo') {
                    script {
                        def imageTag = "v${env.BUILD_NUMBER}"
                        bat "docker build -t ${IMAGE_NAME}:${imageTag} ."
                        bat "docker tag ${IMAGE_NAME}:${imageTag} ${IMAGE_NAME}:latest"
                    }
                }
            }
        }

        stage('Push Docker Image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhub-credentials', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    script {
                        def imageTag = "v${env.BUILD_NUMBER}"
                        bat """
                    echo Logging in to DockerHub...
                    echo %DOCKER_PASS% | docker login -u %DOCKER_USER% --password-stdin
                    docker push ${IMAGE_NAME}:${imageTag}
                    docker push ${IMAGE_NAME}:latest
                """
                    }
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                script {
                    bat '''
                    kubectl set image deployment/demo-deploy demo-container=${IMAGE_NAME}:latest --record
                    kubectl rollout status deployment/demo-deploy
                    '''
                }
            }
        }

        stage('Run Docker Container') {
            steps {
                script {
                    bat """
            docker stop demo-app || true
            docker rm demo-app || true
            docker run -d -p 8080:8080 --name demo-app ${IMAGE_NAME}:latest
            """
                }
            }
        }
    }

    post {
        success {
            echo '✅ Build & Run successful!'
        }
        failure {
            echo '❌ Build failed!'
        }
    }
}
