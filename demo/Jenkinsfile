pipeline {
    agent any

    tools {
        maven 'MAVEN_HOME'
        jdk 'JDK17'
    }

    stages {
        stage('Checkout') {
            steps {
                echo '📦 Cloning GitHub repository...'
                git branch: 'master', url: 'https://github.com/thanh241203/connect-with-jenkin.git'
            }
        }

        stage('Build') {
            steps {
                echo '🔨 Building the project...'
                dir('demo'){
                    echo 'In demo'
                    bat 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Run') {
            steps {
                echo '🚀 Starting application...'
                bat 'java -jar target/demo-0.0.1-SNAPSHOT.jar demo.jar'
            }
        }
    }

    post {
        success {
            echo '✅ Build & Run successful!'
        }
        failure {
            echo '❌ Build failed!'
        }
    }
}
